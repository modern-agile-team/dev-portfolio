export interface TechStackNamePropsType {
  /**
   * Name of skill (find skill from {@link "here"})
   */
  name?: string;
  /**
   * Logo name of skill (find skill from {@link "here"})
   */
  logoName?: string;
  /**
   * Font size of skill name (unit: px)
   */
  fontSize?: string;
  /**
   * Size of logo (unit: px)
   */
  logoSize?: string;
}

export interface TechStackNameContainerStyledPropsType {
  margin?: string;
}

export interface TechStackNameStyledPropsType {
  fontSize?: string;
}

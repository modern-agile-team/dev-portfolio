export interface GapTypes {
  NARROWER: string;
  NARROW: string;
  NORMAL: string;
  WIDE: string;
  WIDER: string;
}

export interface GradationTypes {
  MID_NIGHT: {
    backgroundColor: string;
    color: string;
  };
  BLOSSOM: {
    backgroundColor: string;
    color: string;
  };
  FRUITS: {
    backgroundColor: string;
    color: string;
  };
  BARE_BARE: {
    backgroundColor: string;
    color: string;
  };
  TROPIC_GREEN: {
    backgroundColor: string;
    color: string;
  };
  POPPY: {
    backgroundColor: string;
    color: string;
  };
}

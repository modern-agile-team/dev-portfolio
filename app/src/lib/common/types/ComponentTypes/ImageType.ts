export interface ImagePropsType {
  src: string;
  subhead?: string;
  head?: string;
  redirectURL?: string;
  noShowHead?: boolean;
  zoomWhenHover?: boolean;
}

export interface ImageItemsStyledPropsType {
  noShowHead?: boolean;
  zoomWhenHover?: boolean;
}
